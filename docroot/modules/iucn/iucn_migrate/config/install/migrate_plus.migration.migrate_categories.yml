langcode: en
status: true
id: iucn_migrate_categories
migration_group: iucn migrations
label: Categories
source:
  plugin: iucn_migrate_source_url
  data_fetcher_plugin: http
  data_parser_plugin: json
  urls: /json/iucn_categories
  ids:
    site_category:
      type: string
  fields:
    -
      name: site_category
      label: 'site category'
      selector: category
process:
  vid:
    plugin: default_value
    default_value: category
  name: site_category
destination:
  plugin: 'entity:taxonomy_term'
